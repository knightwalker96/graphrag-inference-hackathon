# LangChain (only the sub-packages actually used)
langchain-core>=0.2.0
langchain-chroma>=0.1.0
langchain-text-splitters>=0.2.0
langchain-ollama>=0.1.0          # optional Ollama judge/answer provider

# Vector store
chromadb>=0.5.0

# Embeddings + cross-encoder reranker (auto-downloaded on first run)
sentence-transformers>=3.0.0

# Dataset + TF-IDF retrieval helpers
datasets>=2.20.0
scikit-learn>=1.3.0

# Token counting
tiktoken>=0.7.0

# Evaluation
evaluate>=0.4.0
bert-score>=0.3.13
huggingface-hub>=0.23.0

# Dashboard
streamlit>=1.35.0
pandas>=2.2.0
plotly>=5.22.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
numpy>=1.26.0
requests>=2.31.0

# Pipeline 3: GraphRAG (TigerGraph) + spaCy NER
# The en_core_web_sm model is installed as a pip dependency so hosted deploys
# (Streamlit Cloud) get it without `python -m spacy download`.
spacy>=3.7.0,<3.8.0
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
pyTigerGraph>=1.0.0

# Answering LLM (OpenAI gpt-4o-mini)
openai>=1.0.0
